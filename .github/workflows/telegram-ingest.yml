name: Telegram Ingest

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - run: pip install requests

      - name: Fetch new transmissions
        if: ${{ secrets.TELEGRAM_BOT_TOKEN != '' }}
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL: '@uvb76logs'
        run: python scripts/telegram_ingest.py

      - name: Commit and push if changed
        if: ${{ secrets.TELEGRAM_BOT_TOKEN != '' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/transmissions.json data/.last_update_id data/UVB76_TELEGRAM_INGEST.md
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Auto-ingest: new UVB-76 transmissions from Telegram"
            git push
          fi
